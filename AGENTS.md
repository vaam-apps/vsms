# vsms — project context

A2P SMS gateway for Cameroon (MTN + Orange). OTP and notification delivery, provider abstraction over HTTP APIs and SMPP, Next.js admin console.

**Read [`docs/architecture.md`](docs/architecture.md) before making design decisions.** It is the spec, and every claim in it was verified against the real toolchain rather than assumed. This file is the subset that must be in context for *every* session.

## Status

**Milestone 0 done.**

- `schema/schema.cstack` — 19 models, 13 enums, 7 procedures. Parses, emits DDL, and expands through `include_server_schema!`.
- `schema/migrations/postgres/0001_init/` — generated by `cratestack migrate diff`. **Never hand-edit.** Regenerated wholesale on every schema change at this pre-production stage — see below.
- `schema/migrations/postgres/0002_bootstrap/` — generated from §2.10 of the design doc by `ci/gen-bootstrap-sql.py`. **Edit the doc, then regenerate.** CI diffs them.
- `crates/sms-encoding/` and `crates/sms-msisdn/` — done: pure, no framework dependency, `cargo test --workspace` green including the French-corpus gate.
- `crates/sms-api/` — **the schema expands.** `include_server_schema!`, the `Principal → CoolContext` projection, the seven-procedure registry, router assembly, `SM001`/`23505` error mapping. 87+ generated routes.
- `app/sms-gateway/` — the binary: `serve` and `routes`.
- `OperatorPrefixRule` — models `sms_msisdn::OperatorPrefixTable`'s backing store, seeded with 14 rows of current best evidence (MTN `67x`/`650`–`654`, Orange `69x`/`655`–`659`, `68x` contested, Camtel `62x` unverified). `previewMessage` still reports `operator: unknown` — the table exists and is seeded, but no procedure queries it yet. That's real work, not a gap in this milestone: `previewMessage` is synchronous and pure today, and querying the DB means giving it a cache and a refresh policy. Natural fit is milestone 2 or 5, alongside the routing code that has the same need.

`previewMessage` is the one live procedure; the other six return an error naming the milestone that will build them.

**Milestone 1 in progress.** The auth shape is decided ([#6](https://github.com/vymalo/vsms/issues/6)): `authkestra-op` stays, pinned `=0.3.3`, and machine callers authenticate with **`private_key_jwt`** — no shared client secret exists anywhere in this system. The `GrantType` `#[serde(untagged)]` bug that shaped the original design is **fixed in 0.3.2** (verified by round-tripping every variant; see §4.2). Schema and migrations for it have landed: `OauthClient` lost `secretHash` and gained `tokenEndpointAuthMethod` + `jwks`, plus new `OauthSigningKey` and `ClientAssertion` models. `/token` rate limiting is explicitly out of scope — it is a reverse-proxy concern, and with no Argon2 at the token endpoint the original urgency is gone.

Three things about that schema are load-bearing and easy to undo by accident:

- **`tokenEndpointAuthMethod` has no `@default` on purpose.** A `@default` would drop it from `CreateOauthClientInput`, and a *missing* method is how authkestra spells "accepts a secret from either transport, refuses assertions" — the one state `private_key_jwt` must never reach. `crates/sms-api/tests/create_inputs.rs` turns its disappearance into a compile error.
- **`@sensitive` on `OauthSigningKey.privateKeyPem` is not confidentiality.** It redacts audit snapshots only; the model's `hasRole('system')` read policy is the entire control. `system` is synthetic and no issued token may carry it — an `AuthProvider` that mints `system` for HTTP callers hands out the key that signs every token, through generated CRUD.
- **`ClientAssertion` is insert-only, written as `create` + catching `23505`.** authkestra requires `record_jti` to be atomic; a read-then-write is the TOCTOU race the table exists to prevent.

`#19`'s `SmsClientStore`/`SmsClientAssertionStore` landed. `#20` (OIDC provider) and `#21` (real `AuthProvider`) landed together in #97, superseding a stale earlier attempt (#85) whose own review (B1–B5, P1–P4) is the source-of-truth for what got fixed: `crates/sms-auth/src/op.rs` — RS256 key management with overlap-window rotation, `CompositeOpStore` assembly, `OpConfig`; `crates/sms-api/src/auth.rs`'s `GatewayAuth` replaces `DenyAll`; `app/sms-gateway/src/op.rs` mounts the OP's three routes plus a `rotate-signing-key` CLI subcommand — deliberate, per §4.2 the OP's signing key is not a schema default, so an operator must run it once before `/token` can issue anything (it fails loudly on the first `/token` request if no active key exists, not at process start). Found live, not by reading source: leaving `jsonwebtoken::Validation::set_audience` uncalled does **not** skip audience validation the way it does inside `authkestra_resource::jwt::JwtStrategy`'s own construction — the library still requires an explicit match once a token carries an `aud` claim, which every `client_credentials` token does; fixed with an explicit `validation.validate_aud = false`. Also found live: `GET /oauth_signing_keys` under role `"app"` returns zero rows, not a `403` — CrateStack's list-route policy denial is row-level filtering to an empty array, and `crates/sms-auth/tests/oidc_flow_live.rs` asserts that real shape rather than the assumed one. Scope cut, explicit in `sms_auth::op`'s module doc: no authorization-code/human-login flow (no admin console exists yet to need it) and no scope/permission-layer (§4.6 Layer 2) enforcement — machine callers only, tracked separately as #23/#24/#25.

#98 closed a gap found while closing out #18's own checklist after #97, not caught by #97's bot review: the discovery document at `/.well-known/openid-configuration` never advertised `private_key_jwt` in `token_endpoint_auth_methods_supported`, even though `/token` had accepted it since #97 merged. `authkestra_axum::op::axum_discovery_handler` builds the document straight from `OidcDiscovery::from_config` with no hook to chain `authkestra_op::handlers::discovery::OidcDiscovery::with_private_key_jwt()` onto the result, so `app/sms-gateway/src/op.rs` now hand-rolls `discovery_handler`, the same pattern the file already uses for `jwks_handler` (the library handler can only ever publish one signing key). `oidc_flow_live.rs` duplicates `op.rs`'s route assembly — `app/sms-gateway` is a binary crate with no `lib.rs`, so its modules aren't importable from a test in `sms-auth` — and carries a matching permanent assertion that fetches the live discovery document over real HTTP and checks the advertised methods, not just a unit test of `OidcDiscovery` in isolation.

M1 is still open: #23 (`provisionAppClient` service-account provisioning), #24 (RBAC layers 1 and 2), #25 (gate: a persisted `client_credentials` client gets a token).

**`aws-lc-rs` is in this tree from four independent paths, and authkestra is only one of them.** [marcjazz/authkestra#179](https://github.com/marcjazz/authkestra/pull/179) (in 0.3.3) made authkestra's TLS backend a feature so consumers can opt out of `aws-lc-rs` — but that alone does **not** remove it here. `cargo tree -i aws-lc-rs -e features` shows it also arriving via `cratestack-client-rust` → `reqwest 0.12`, `sqlx-core`, and `rustls-platform-verifier`. Cargo features are additive and unified: as #179's own description puts it, if anything in the graph still enables `rustls-aws-lc-rs`, it comes back. So a static musl/`scratch` build, or a `cargo-deny` policy banning `aws-lc-rs`, needs the *cratestack* side to offer the same opt-out first — bumping authkestra is necessary but nowhere near sufficient. Don't claim the bump removes it.

Note also two `reqwest` majors coexist (0.12 via cratestack, 0.13 via authkestra). Pre-existing, harmless today, but it means TLS-feature reasoning has to be done per-major.

**`#87` is fixed — and it is the reference example for why this repo distrusts a green `cargo test`.** Found while verifying `#19` against a live database: `cratestack-sqlx` `=0.5.0`–`=0.5.2` discarded SQLSTATE/constraint on *every* generated write, so `map_database_error`'s `SM001`→409 mapping and `is_illegal_transition` silently did nothing against a real database, despite `#78` shipping specifically to guarantee them. Filed as [cratestack/cratestack#267](https://github.com/cratestack/cratestack/issues/267), fixed in `cratestack-sqlx` 0.6.0 (all twelve write paths now route through `cool_error_from_sqlx`); the pin is `=0.6.3`.

The reason it went unnoticed through `#78` is worth internalising: every test of that mapping constructed `CoolError::DatabaseTyped` **by hand**, which is correct unit coverage of the mapping function and structurally incapable of seeing the conversion feeding it. `cargo build`/`cratestack check` can't see it either — the signature is `Result<T, CoolError>` either way. `crates/sms-api/tests/errors_live_postgres.rs` now covers it end to end through a real delegate call, and was confirmed to fail against `=0.5.2` and pass against `=0.6.3` before being trusted. **Never pin the cratestack family below 0.6.0.**

**Milestone 2 in progress.** `crates/sms-worker` (library) + `app/sms-worker` (binary, package `sms-worker-bin` — see its module doc for why the package name differs from the produced executable) exist: `--roles` parsing and validation, the six roles from §7.1 with their `Cardinality`, and a `run(role)` per-role stub that logs once and then genuinely idles (`std::future::pending`) rather than busy-looping, matching `sms-api`'s "clearly-labelled, not a lying stub" convention for the six unimplemented procedures (`#27`).

Leader election landed on top of that (`#28`): `crates/sms-worker/src/lease.rs` — an R1 exception, allowlisted by path in `ci/assert-no-raw-sqlx.sh`. Deliberately **doesn't** use §7.2's own illustrative `pool.acquire()` sample — a `PoolConnection` returns to the pool rather than closing on drop, so a lease dropped without an explicit `release()` (a panic, a `kill -9`) would leak the lock at the Postgres level forever, unreachable, on a session silently recycled to serve unrelated future queries. Verified live that this trap is real (a second, independent connection stayed unable to acquire a lock left behind by a dropped `PoolConnection`) and that `RoleLease` avoids it (a dedicated, never-pooled `PgConnection` — dropping it closes the socket immediately, and Postgres's own session-advisory-lock semantics release the lock the moment that session ends, no cooperation from `sqlx` required). `run_singleton` wires this into a retry loop (5s interval, per §7.2) behind a `CancellationToken`, releasing explicitly on SIGINT/SIGTERM as the fast path and falling back to the drop-triggered release on a hard kill. Manually verified end to end against a real two-node setup: standby correctly stands by while the other holds the lock, `kill -9` failover reclaims within one retry cycle (~5s, no cooperation from the killed process needed), graceful SIGTERM triggers explicit release before exit.

The CAS claim loop landed next (`#29`): `crates/sms-worker/src/claim.rs`. A `Claimable` trait — `candidates()` plus `take_lease()` via `if_match(version)` — so the loop that actually matters (treat `PreconditionFailed` as a lost race and retry, log `Forbidden` loudly rather than folding it into the race branch, propagate anything else) is written once and reused per model; `Job` (`#35`) and `WebhookAttempt` (M3 `#40`) each add their own `impl Claimable` later rather than re-deriving this. `impl Claimable for Message` is §7.3's own worked example, with one correction: the candidate state filter is `accepted`/`queued`/**`routed`**, not just the first two — §7.3's own sample omits `routed`, but `take_lease` transitions into it, `routed -> queued` is a legal edge (§7.4), and `messages_lease_reclaim_idx` (§2.10, committed since milestone 0) is already built `WHERE ... state IN ('queued','routed')`. Without `routed` in the filter, a worker that crashes between claiming a message and finishing it leaves that row permanently stuck — caught by Greptile's review of `#93`, confirmed against the doc's own index before fixing.

`#30`–`#32` landed together in #94: `crates/sms-provider` (the `SmsProvider` trait, `Capabilities`, a `ProviderError` taxonomy with a compiler-checked mapping to routing consequences), `crates/sms-provider-orange-cm` (the Orange CM HTTP adapter, OAuth2 `client_credentials` token caching), and `sendMessage` itself — replacing the milestone-not-reached stub with §3.2's nine pre-persistence steps, live in `Procedures::send()`. A new generic `TtlCache<K,V>` (`crates/sms-api/src/cache.rs`) backs both the client_id→App and operator-prefix lookups. Found live by the new `send_message_live_postgres.rs` (9 tests), not by inspection: four pre-existing schema policy gaps, all the same shape — `AppClient` create, `App`/`AppClient` read, `SenderIdRegistration` read, `OperatorPrefixRule` read were all missing `hasRole('system')`, which `sendMessage`'s internal system-context logic needs. Each fix was verified individually to produce a byte-identical DDL diff via `cratestack migrate diff` before moving to the next — this is the first of what turns out to be a recurring bug shape through the rest of M2.

`#33` — `Role::Dispatch`'s real body — landed in #96: `crates/sms-worker/src/dispatch.rs`, the poll loop wired over #29's claim loop and #94's provider abstraction, walking `accepted -> queued -> routed -> submitted` per §7.4, with `ProviderError` classified onto the failure edges. Single-provider degradation is deliberate and documented, not a gap: `Permanent`/`Unavailable`/`TryNextRoute`/`OpenCircuitAndTryNextRoute` all collapse onto whichever §7.4 edge fits, since no second provider exists yet for real failover (M5, `#63`). Two real bugs found landing it: `claim.rs`'s `take_lease` wrote `-> routed` unconditionally regardless of the candidate's actual state, making the illegal edge `accepted -> routed` reachable; and — the more consequential one — **`#29`'s own live suite (`claim_live_postgres.rs`, 7 tests, previously written but never run against Postgres) ran for the first time in #96**, which is what surfaced that `Message`'s `list`/`detail` policies had never admitted `hasRole('system')` at all: the claim loop's own query had silently returned zero rows since `#29` merged, undetected because nothing had run that suite live until now. Both fixed; `claim_live_postgres.rs`, the new `dispatch_live_postgres.rs` (5 tests, `wiremock`-backed `OrangeCmProvider`), and `send_message_live_postgres.rs` all pass stably across interleaved reruns against the same persistent database.

`#34` — DLR ingestion — landed in #99: `POST /dlr/{providerKey}`, a raw axum route rather than CrateStack-routed, since a provider webhook carries no bearer token to validate against `GatewayAuth`. `crates/sms-api/src/dlr.rs` matches a `DeliveryUpdate` against a `Message` by `providerMessageRef`/`providerMessageRefAlt`, writes the `DeliveryReceipt`, and drives the resulting transition; `app/sms-gateway/src/dlr.rs` owns route wiring and provider-key dispatch, the same procedure-vs-router split `sendMessage` already uses. One correctness case the design doc's transition table implies but doesn't spell out, caught by the live suite (`dlr_ingestion_live_postgres.rs`, 5 tests): a retryable failure arriving while a message is `uncertain` must land in `failed`, not `undelivered` — `uncertain -> undelivered` isn't a legal edge in `message_state_transitions`. Explicitly out of scope, documented in `dlr.rs`'s own module doc: `undelivered -> queued` retry has no driver yet (`claim.rs`'s `candidates()` doesn't select `undelivered`), and DLR webhook signature verification — no scheme is documented for any provider yet, though `RawCallback` already preserves exact bytes for when one lands.

`#35` — the `jobs` role and generic job queue — landed in #100: `Claimable for Job` (`crates/sms-worker/src/claim.rs`) adapts the same CAS discipline to `Job`'s own crash-reclaim semantics, `running -> pending`, a two-hop reclaim rather than `Message`'s same-state resume — proven live by asserting the handler isn't called on the reclaim tick, only on the claim after it. `Role::Jobs`'s real body (`crates/sms-worker/src/jobs.rs`) claims, dispatches by `kind` through a `JobHandler` registry, and transitions on the outcome with backoff/`dead` handling; `Role::Scheduler`'s real body (`crates/sms-worker/src/scheduler.rs`) enqueues due recurring jobs with singleton-owned, in-process cadence tracking — correct as long as exactly one instance holds the lease, and its dedupe path knowingly inherits `#87`'s already-documented `db_sqlstate()` gap (blast radius: a spurious error log, not data corruption, since the DB's unique index still blocks the insert either way). Exactly one real job kind is wired up for this MVP, `expire_stale` (`crates/sms-worker/src/jobs/expire_stale.rs`, two rules: `submitted` past `expiresAt`, `uncertain` past its 6h grace) — the other eight §7.5 names (`poll_balance`, `probe_providers`, `reap_outbox`, `reconcile_clients`, `purge_retention`, `cleanup_secrets`, `anchor_audit`, `verify_backup`) are explicitly out of scope, each blocked on infrastructure this milestone doesn't build. Per #100's own count, `Job.list`/`detail` missing `hasRole('system')` was the **sixth** instance of the exact same policy-gap shape (`App`, `AppClient`, `SenderIdRegistration`, `OperatorPrefixRule`, `Provider` before it) — same fix, same byte-identical-DDL verification each time.

`#102` is fixed (each live-Postgres test binary now serializes against itself — see `ca653a1`).

`#36` (the M2 gate) has its automatable half done: `app/sms-worker/tests/kill9_reclaim_live.rs` spawns the real `sms-worker` binary against a live Postgres and a wiremock-delayed Orange stand-in, sends it a genuine `SIGKILL` mid-submit (not an in-process simulation — `Child::kill()`, so no Rust destructor ever runs), and proves the row survives as `routed` with no `providerMessageRef`, then that a second, independent process reclaims and resubmits it (same `attempts`, a new `providerMessageRef`). It also pins down, as a permanent regression assertion rather than a one-off manual finding: the resubmit is a genuinely new outbound call, so a crash in this exact window produces two real submissions — `providerMessageRef` has no DB-level uniqueness constraint and nothing today gives Orange a dedup key. That's a tracked, accepted gap, not something this test hides. `docs/runbooks/36-handset-gate.md` (from `#106`) covers what can't be automated — a real Orange handset receiving `delivered` within 15s, and a human-timed `kill -9` against a real (not mocked) Orange account — and stays the actual gate for closing `#36`; both remain to be run by a human with real credentials and a real phone.

## Regenerating migrations — this repo's actual workflow, found the hard way

There is no committed `schema.snapshot.json` baseline, so `cratestack migrate diff` has nothing to diff *against* — every invocation emits the **complete current schema**, not an incremental delta. `0001_init` already followed this shape before anyone here made it a deliberate policy: it contains all 16 (now 19) `CREATE TABLE`s in one file. That's fine and intentional at this stage — there is no live database anywhere, so there is no data-preservation reason to want a true incremental migration.

So: when the schema changes, **regenerate `0001_init` wholesale** with the exact command in §2.10, don't try to hand-write an incremental one. Then regenerate `0002_bootstrap` from the doc. Both from scratch, both committed, every time:

```bash
cratestack migrate diff --schema schema/schema.cstack --out-dir schema/migrations --backend postgres --name init
# copy the output over schema/migrations/postgres/0001_init/{up,down}.sql
python3 ci/gen-bootstrap-sql.py schema/migrations/postgres/0002_bootstrap/up.sql
```

The tool also writes `schema/migrations/postgres/schema.snapshot.json` as a side effect. Delete it — this repo doesn't use snapshot-based incremental diffing, and a stale one committed by accident would be misleading.

**Verify against a real database before trusting the regeneration**, not just `cratestack check` — "parses" is not "compiles" is not "applies". `docker run --rm -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres:16`, apply both migrations in order, then run `ci/test-state-machine.sql`. This is how the 0.5.0 enum-type change below was actually caught — `cratestack check` and `cargo build` both stayed green through it.

## Invariants that fail the build rather than production

Three silent failure modes are now loud. Do not delete these without reading why they exist:

- **`ci/assert-state-machine-parity.py`** (R2) — every edge in the design doc's state diagrams must exist in `message_state_transitions` / `job_state_transitions`, and vice versa, terminal states included. Drift's first symptom would otherwise be a production `SM001` on an edge that looks legal in the diagram everyone reads. Run with `just parity`.
- **`crates/sms-api/tests/create_inputs.rs`** — constructs every create input exhaustively, with no `..Default::default()`. Adding a `@default(...)` to a caller-settable field silently removes it from `CreateXInput`; `@server_only` removes it from create *and* update (R3). Both become compile errors naming the field.
- **`crates/sms-api/src/errors.rs`** — `map_database_error` turns SQLSTATE `SM001` into `CoolError::Conflict` (409) and `23505` into a named conflict. Untranslated, an illegal transition is a `500 DATABASE_ERROR`, which callers retry and operators read as a gateway fault. Genuine database faults stay 500 on purpose.

## Layout

`crates/` is libraries, `app/` is binaries, and the dependency arrow only points from `app/` into `crates/`. Internal crates are wired through `[workspace.dependencies]` path entries, so versions and features are set once at the root. Same split as `lightbridge-authz`, for the same reasons. Full tree in [§11](docs/architecture.md#11-repository-layout).

## Build cost — fixed upstream, keep the pin

**Do not drop below `cratestack =0.5.0`.** On 0.4.x, `include_server_schema!` expansion was **exponential in relation-graph connectivity**, and this schema could not be compiled at all on a 32 GB machine: rustc was SIGKILLed after 20 min 53 s having driven swap from 20 GB to 45 GB. Measured with synthetic chained schemas, `cargo check`, same machine:

| chained models | 0.4.19 | 0.5.0 |
|---|---|---|
| 4 | 13.4 s / 1.70 GB | 8.1 s / 0.48 GB |
| 6 | 568.6 s / 10.49 GB | 1.9 s / 0.40 GB |
| 8 | >600 s, unfinished | 2.5 s / 0.46 GB |
| 16 | — | 4.3 s / 0.71 GB |

Cause was a path-local rather than memoised recursion guard in `cratestack-macros/src/relation/recursive_entries.rs`: `visited` was cloned per branch, so every distinct path through the relation graph re-expanded the whole nested module subtree. Reported as [cratestack#252](https://github.com/cratestack/cratestack/issues/252), fixed in 0.5.0. The whole workspace now builds in ~21 s.

Retained because they are cheap and still correct, not because they are load-bearing any more:

- `[profile.dev] debug = "line-tables-only"` — panic backtraces keep file and line, variable/type DWARF is dropped.
- `[profile.release]` is `thin` / `opt-level = 3` / `codegen-units = 16`, with a `[profile.dist]` for shipped artifacts. Fat LTO plus one codegen unit is the worst case for codegen memory — see [cratestack#250](https://github.com/cratestack/cratestack/pull/250). Note a dependency's profile never reaches us; Cargo honours profiles only from the root workspace manifest, so that setting spreads by copy-paste.
- `CARGO_BUILD_JOBS=4` and `CARGO_INCREMENTAL=0` in CI, and `just` recipes cap jobs locally. Raise with `just jobs=8 check`.

## Stack, pinned

| | |
|---|---|
| [CrateStack](https://cratestack.dev/) `=0.5.0` | Schema-first. `.cstack` generates models, policies, audit, events, REST. Pre-1.0 and moving fast — pin exactly. Never below 0.5.0; see the build-cost section. |
| [Authkestra](https://github.com/marcjazz/authkestra) `=0.3.3` | OIDC provider. 0.3.2 fixed the `GrantType` serde bug that shaped early M1 design (#6). Pin exactly — still moving fast, and `authkestra-macros` is transitive, so `cargo update -p authkestra-macros` is needed to keep the family in true lockstep. |
| PostgreSQL 16 | The only coordination mechanism. Queues (`SKIP LOCKED`), leader election (advisory locks), state machines (triggers). No broker, no Redis. |
| Rust 2021, TypeScript / Next.js 15 | |

`sms-api` depends on `cratestack = { package = "cratestack-pg", version = "=0.5.0" }` — the rename is mandatory, generated code emits absolute `::cratestack::*` paths. `JsonCodec` lives in the separate `cratestack-codec-json` crate.

## The three rules

Full reasoning in [CONTRIBUTING.md](CONTRIBUTING.md). In short:

- **R1 — all data access goes through CrateStack delegates. Never raw `sqlx`.** Raw SQL bypasses row-level policy, audit rows, `@@emit` outbox rows and `@version` bumping — all four, silently. Three named exceptions only: migrations, `pg_advisory_lock`, `LISTEN`/`NOTIFY`. Enforced by `ci/assert-no-raw-sqlx.sh`.
- **R2 — state transitions are proposed by Rust and decided by Postgres.** Legal edges live in `message_state_transitions` / `job_state_transitions`; triggers reject the rest with SQLSTATE `SM001`. Map it to `CoolError::Conflict` → 409.
- **R3 — nothing that must be written can be `@server_only`.** It excludes a field from create *and* update, so such a field can never be populated.

## Framework constraints that are not in any documentation

Each of these was found by running the toolchain. Each will silently waste an afternoon. Full table with exact error text in [§2.0](docs/architecture.md#20-grammar-and-emitter-constraints).

**Will not compile / will not parse:**

- **Scalar list fields panic the server macro** — `String[]` / `Int[]` give `proc macro panicked: unsupported SQLx value type for this slice`. The parser and migration emitter accept them happily; only `include_server_schema!` fails. Multi-values here are space-delimited strings with **sentinel separators** (`" a b "`, so `.contains(" a ")` can't false-match `ab`).
- **Exactly one whitespace character** between field name and type. No column alignment — `id  Cuid` fails with `invalid type reference:`.
- **The parser is line-based.** An `@@allow` cannot wrap onto a second line.
- **Relation scalars and policy comparisons require type-name equality.** A `String` FK to a `Cuid` PK is a hard error, including through `auth().x` comparisons.

**Will compile, and be silently wrong:**

- **`@@use` instead of `@use`** parses, is retained as an unknown attribute, and does not expand the mixin. No error.
- **A misspelled `@@allow` action is dropped**, and deny-by-default makes that operation unreachable.
- **Unknown attributes are ignored** — a typo'd attribute is a no-op.
- **Enum `@default` values are not checked against variants** — a typo reaches the DDL and fails at `psql` time.

**Shape the schema:**

- **Any `@default(...)` excludes the field from `CreateXInput`** — literals included, not just `dbgenerated()`. A `@default` on a caller-settable field makes it unwritable. Kept only where unsettable is the control: `Message.state @default('accepted')` is why no client can create a message that is already `delivered`.
- **Defaulted fields *are* settable on update.** So `@default(auth().x)` protects creation only.
- **`@pii` / `@sensitive` redact audit snapshots only** — they add no serde attribute, so the field is still returned by the API. Not confidentiality controls.
- **Ids must be lowercase alphanumeric, no separator.** `Cuid` is format-guarded `[a-z0-9]{2,32}` on REST query filters, so a `msg_` prefix makes `GET /messages?id=…` return 400. `cs_cuid()` emits 23 chars.
- **No `@@index`, no foreign keys, no triggers, no column defaults** come out of the emitter — all hand-written in `0002_bootstrap`.
- **`pluralize()` is naive** (`ends_with('s') ? +"es" : +"s"`) and there is no `@@map`. `WebhookDelivery` would become `webhook_deliverys` — the model is `WebhookAttempt` for that reason.
- **`upsert` doesn't exist when the `@id` has a default.** Dedupe is `create` + catching SQLSTATE `23505` via `err.db_sqlstate()`.
- **`SKIP LOCKED` is not expressible.** `.for_update()` is. Claim loops use optimistic CAS on `@version`, treating `PreconditionFailed` as "another worker won" — and **`Forbidden` is ambiguous** (policy denied *or* row gone), so log it rather than folding it into the race branch.
- **`@db_enforce` backs `@length`/`@range` with a real `CHECK` and is a silent no-op on `@regex`.** No error, no warning — verified with an isolated probe schema. Anything pattern-shaped needs a hand-written `CHECK (col ~ '...')` in `0002_bootstrap` instead.
- **0.5.0 changed enum-typed columns from native `CREATE TYPE ... AS ENUM` to `TEXT` + `CHECK (col IN (...))`, silently.** Anything referencing the old type name directly — our `message_state`/`job_state` transition-table columns were — breaks at `psql` time on the next full regeneration with `type "message_state" does not exist`. `cratestack check` and `cargo build` both stay green through it; only applying the migration for real catches it.

## Milestone 0 crates — what was non-obvious

`sms-encoding` and `sms-msisdn` are done ([§3.3](docs/architecture.md#33-encoding--write-this-crate-first), [§3.4](docs/architecture.md#34-msisdn-normalisation)). Findings that weren't in the design doc's prose and would otherwise get rediscovered the hard way:

- **Segment counting is a packing loop, not division.** A GSM-7 escape pair (`€`, `[`, …) or a UTF-16 surrogate pair (emoji) must not straddle a segment boundary. `ceil(length / per_segment)` undercounts by one on exactly the bodies where the straddle happens — see `crates/sms-encoding/src/segment.rs`.
- **Most uppercase accented vowels are *not* in GSM-7, even though the lowercase forms are.** `à è ù ì ò` are single septets; `À È Ù Ì Ò` are not — only `É` made it into the default alphabet. A body that's one segment in sentence case can become UCS-2 the moment it's upper-cased (e.g. shouty confirmation codes). `Ä Ö Ñ Ü` and their lowercase forms are all encodable either case.
- **Normalisation and transliteration are deliberately different passes, and the split matters for correctness, not just policy.** `normalise` (unconditional) only touches characters that are typographically identical to a GSM-7 character — `’→'`, en/em dash → `-`, NBSP → space. `transliterate_to_gsm7` (opt-in per `App.transliterateToGsm7`) additionally strips accents via NFD decomposition + combining-mark removal, which is *perceptible* (`ç→c`) and must never run by default.
- **Cameroon's numbering plan puts a chunk of mobile numbers in the `2xx` range, not `6xx`.** Camtel mobile is `242`/`243`, sitting inside the fixed-line leading digit `2`. A classifier that assumes "starts with 6 = mobile, starts with 2 = fixed" misclassifies it.
- **`63x` and `643`–`649` are syntactically well-formed 9-digit numbers in no assigned block.** They need a distinct outcome (`Unallocated`) from both "wrong length" and "known-but-not-mobile" — collapsing them into a generic parse failure loses the operator-relevant distinction.
- **The `88x` toll-free/shared-cost range is 6–7 digits after the prefix, i.e. 8 total digits, not 9** — the one legitimate 8-digit number in the plan. It has to be special-cased before the generic "8 digits = pre-2014 legacy number" rejection, or every toll-free number gets misdiagnosed as a legacy number that needs a `6`/`2` prefix guess.
- **`OperatorPrefixTable` ships with zero built-in rows on purpose** ([§3.4](docs/architecture.md#34-msisdn-normalisation) is explicit that prefix routing "must never be load-bearing"). The table type does longest-prefix-match lookup only; the actual MTN/Orange/Camtel prefix data belongs in the database, seeded and corrected from observed DLRs, not compiled into the crate.

## Verified toolchain API (don't guess these)

```rust
db.message().find_many()
    .where_expr(FilterExpr::from(message::state().in_([MessageState::accepted]))
                  .and(message::expiresAt().gt(now)))
    .order_by(message::priority().desc())   // appends, doesn't replace
    .limit(n).run(&ctx).await?

db.message().update(id).set(UpdateMessageInput { ..Default::default() })
    .if_match(version).run(&ctx).await?     // required on @version models
```

- Filter helpers: `cratestack_schema::<model_snake>::<fieldNameVerbatimCamelCase>()`.
- Ops: `.eq .ne .lt .lte .gt .gte .in_ .is_null .is_not_null .eq_or_null .contains .starts_with .is_true .is_false`. No `not_in`, `between`, `ilike`.
- `.and()` / `.or()` are on `FilterExpr`, not `Filter` — wrap with `FilterExpr::from(...)`. Separate `where_*` calls are always ANDed.
- Enum variants are lowercase-verbatim: `MessageState::accepted`. `Int` is `i64`. Nullable setters are `Option<Option<T>>`.
- Transactions: every builder has `.run_in_tx(&mut tx, &ctx)`; audit and outbox rows join that transaction. `cratestack::run_in_isolated_tx(pool, isolation, closure)` — the closure takes an owned tx and must return `(T, tx)`.
- `@@emit` is a **real transactional outbox** (row written in-tx, survives process death), but delivery is **synchronous, blocking the mutation, and not panic-isolated**, with no `SKIP LOCKED` on drain and no retry cap. Subscribers must be trivial, idempotent, and must not panic.

## Commands

```bash
# prefer just — the recipes cap CARGO_BUILD_JOBS, bare cargo does not
just check          # cargo check --workspace --all-targets
just test
just lint           # fmt --check, then clippy -D warnings
just all-checks     # everything CI runs, in CI's order
just routes         # print the generated route table; no database needed
just jobs=8 check   # raise the concurrency cap on a big machine

# apply migrations to a scratch database
createdb vsms_check
DATABASE_URL=postgres://localhost/vsms_check ./ci/apply-migrations.sh
psql postgres://localhost/vsms_check -v ON_ERROR_STOP=1 -f ci/test-state-machine.sql
dropdb vsms_check

# after editing schema.cstack
cratestack migrate diff --schema schema/schema.cstack \
  --out-dir schema/migrations/postgres --backend postgres --name <change>

# after editing §2.10 of the design doc
python3 ci/gen-bootstrap-sql.py schema/migrations/postgres/0002_bootstrap/up.sql

./ci/assert-no-raw-sqlx.sh
```

`cratestack migrate diff` has **no `--check` or dry-run mode**; its only flags are `--schema`, `--out-dir`, `--backend`, `--name`, `--allow-destructive`.

**"Parses" is not "compiles."** `cratestack-parser` and `cratestack-migrate` both accept schemas that `include_server_schema!` refuses. Once `crates/` exists, `cargo check -p sms-api` is the real gate. Expanding all 16 models is memory-hungry — budget for it.

## Conventions

- Commits: imperative subject, body explaining *why*. Record framework surprises in the commit body and in §2.0 — that table is the most valuable thing here for whoever comes next.
- Schema changes ship with their migration and a design-doc note if a decision changed.
- New R1 exceptions need reasoning in the PR, not just an allowlist edit.

## Open questions blocking later milestones

1. **Hosting location.** Law No. 2024/017 requires prior authorisation for *all* cross-border personal-data transfers, and "legitimate interest" is not a lawful basis. Cameroon-hosted is the safe default. Needs an answer before production.
2. **Own ART title?** Determines whether direct SMPP (milestone 7) exists at all.
3. **10-year traffic retention (Law 2010/012 art. 25) vs 90-day minimisation.** Split-ledger proposal in §10 needs a lawyer.
4. **Does `authkestra-op` stay?** All machine auth runs through it, and it needs a custom `ClientStore` to work around a grant-type authorisation bug, plus `/token` rate limiting it lacks, with no revocation endpoint and no proof-of-possession. Compare against Keycloak / ZITADEL before milestone 1, not after.