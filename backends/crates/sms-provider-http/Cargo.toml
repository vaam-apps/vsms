[package]
name = "sms-provider-http"
description = "Shared HTTP-transport classification for every SmsProvider adapter that speaks HTTP over reqwest — kept out of sms-provider itself so an SMPP-only adapter never pulls in an HTTP client it will never use"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish.workspace = true

[dependencies]
sms-provider.workspace = true
reqwest.workspace = true

[dev-dependencies]
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
wiremock.workspace = true

[lints]
workspace = true
