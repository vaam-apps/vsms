// Server component shell (#58). No filters/`nuqs` state to hold in the URL.

import { OptOutsScreen } from "./opt-outs-screen";

export default function OptOutsPage() {
  return <OptOutsScreen />;
}
