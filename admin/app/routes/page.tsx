// Server component shell (#54). No filters/`nuqs` state — same reasoning
// `providers/page.tsx` gives.

import { RoutesScreen } from "./routes-screen";

export default function RoutesPage() {
  return <RoutesScreen />;
}
